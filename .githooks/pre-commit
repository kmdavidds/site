#!/usr/bin/env node
import { execFileSync } from 'node:child_process';

execFileSync(process.execPath, ['scripts/render-resume.mjs'], { stdio: 'inherit' });
execFileSync('git', ['add', '-A', 'public/resume'], { stdio: 'inherit' });
